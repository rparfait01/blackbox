{
  "name": "@blackbox/api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "build": "wrangler deploy --dry-run --outdir dist",
    "deploy": "node ../../scripts/deploy-worker.mjs",
    "lint": "eslint src",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "acceptance": "node test/acceptance.mjs",
    "build:verifier": "node scripts/build-verifier.mjs"
  },
  "dependencies": {
    "@blackbox/shared": "workspace:*",
    "@simplewebauthn/server": "^13.3.2",
    "hono": "^4.6.16",
    "qrcode-generator": "^2.0.4"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250109.0",
    "typescript": "^5.7.2",
    "vitest": "^2.1.9",
    "wrangler": "^3.99.0",
    "esbuild": "^0.21.5"
  }
}
