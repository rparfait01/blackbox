/**
 * Empty settings placeholder. Reserved for a later phase — no UI is implemented
 * in W1. Nothing in the visible app links here; it exists only as a route stub.
 */
export function Settings(): JSX.Element {
  return <div className="h-full w-full bg-background" />;
}
